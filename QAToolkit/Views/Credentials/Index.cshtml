@model IEnumerable<QAToolkit.Models.Credential>
@{
    ViewData["Title"] = "Credentials";
}

<div class="page-header">
    <div>
        <h1>Credentials</h1>
        <p>Test account credentials organized by project and environment.</p>
    </div>
    <div class="page-header-actions">
        <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Add Credential</a>
    </div>
</div>

<form method="get" class="toolbar">
    <span class="toolbar-label">Type</span>
    <select name="type" class="form-select">
        <option value="">All</option>
        @foreach (var t in ViewBag.Types as List<string?>)
        {
            <option value="@t" selected="@(ViewBag.SelectedType == t)">@t</option>
        }
    </select>
    <span class="toolbar-label">Environment</span>
    <select name="environment" class="form-select">
        <option value="">All</option>
        @foreach (var env in ViewBag.Environments as List<string?>)
        {
            <option value="@env" selected="@(ViewBag.SelectedEnvironment == env)">@env</option>
        }
    </select>
    <button type="submit" class="btn btn-sm btn-secondary"><i class="bi bi-funnel"></i> Filter</button>
    <a href="/Credentials" class="btn btn-sm btn-ghost">Clear</a>
</form>

<!-- Credentials List -->
@{
    var usernameTypes = Model.Where(c => c.Type == "UMS" || c.Type == "Scheduler" || c.Type == "OM" || c.Type == "API" || c.Type == "App").ToList();
    var studentPortalTypes = Model.Where(c => c.Type == "Student Portal").ToList();
    var teacherPortalTypes = Model.Where(c => c.Type == "Teacher Portal").ToList();
    var otherTypes = Model.Where(c => c.Type != "UMS" && c.Type != "Scheduler" && c.Type != "OM" && c.Type != "API" && c.Type != "App" && c.Type != "Student Portal" && c.Type != "Teacher Portal").ToList();
}

@if (!Model.Any())
{
    <div class="card">
        <div class="empty-state">
            <div class="empty-state-icon"><i class="bi bi-shield-lock"></i></div>
            <h5>No credentials yet</h5>
            <p>Store your first test account credentials.</p>
            <a asp-action="Create" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg"></i> Add Credential</a>
        </div>
    </div>
}

@if (usernameTypes.Any())
{
    <div class="card mb-4">
        <div class="card-header bg-light border-bottom">
            <h6 class="mb-0 fw-bold text-dark"><i class="bi bi-person-circle"></i> Username Based Credentials</h6>
        </div>
        <div class="table-responsive">
            <table class="table table-hover table-bordered mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th class="text-center" style="width: 12%;">Type</th>
                        <th class="text-center" style="width: 22%;">Username</th>
                        <th class="text-center" style="width: 18%;">Password</th>
                        <th class="text-center" style="width: 20%;">URL</th>
                        <th class="text-center" style="width: 12%;">Environment</th>
                        <th class="text-center" style="width: 16%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in usernameTypes)
                    {
                        <tr>
                            <td class="text-center">@item.Type</td>
                            <td class="text-center">
                                @item.Username
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Username', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                <span class="password-hidden" id="pwd-@item.Id">********</span>
                                <span class="password-visible d-none" id="pwd-show-@item.Id">@item.Password</span>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="togglePassword(@item.Id)" title="Show/Hide">
                                    <i class="bi bi-eye" id="eye-@item.Id"></i>
                                </button>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Password', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Url))
                                {
                                    <a href="@item.Url" target="_blank" class="text-decoration-none" title="@item.Url">
                                        <i class="bi bi-link-45deg"></i> @(item.Url.Length > 30 ? item.Url.Substring(0, 30) + "..." : item.Url)
                                    </a>
                                }
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Environment))
                                {
                                    var envClass = item.Environment?.ToLower() switch
                                    {
                                        "dev" => "badge-env-dev",
                                        "live" => "badge-env-prod",
                                        "alpha" => "badge-env-qa",
                                        "rc" => "badge-env-staging",
                                        _ => "bg-secondary"
                                    };
                                    <span class="badge @envClass">@item.Environment</span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary me-1" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                      data-confirm="Delete this credential? This action cannot be undone." data-confirm-title="Delete credential">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@if (studentPortalTypes.Any())
{
    <div class="card mb-4">
        <div class="card-header bg-light border-bottom">
            <h6 class="mb-0 fw-bold text-dark"><i class="bi bi-mortarboard"></i> Student Portal Credentials</h6>
        </div>
        <div class="table-responsive">
            <table class="table table-hover table-bordered mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th class="text-center" style="width: 12%;">Type</th>
                        <th class="text-center" style="width: 22%;">Registration Number</th>
                        <th class="text-center" style="width: 18%;">Password</th>
                        <th class="text-center" style="width: 20%;">URL</th>
                        <th class="text-center" style="width: 12%;">Environment</th>
                        <th class="text-center" style="width: 16%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in studentPortalTypes)
                    {
                        <tr>
                            <td class="text-center">@item.Type</td>
                            <td class="text-center">
                                @item.Username
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Username', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                <span class="password-hidden" id="pwd-@item.Id">********</span>
                                <span class="password-visible d-none" id="pwd-show-@item.Id">@item.Password</span>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="togglePassword(@item.Id)" title="Show/Hide">
                                    <i class="bi bi-eye" id="eye-@item.Id"></i>
                                </button>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Password', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Url))
                                {
                                    <a href="@item.Url" target="_blank" class="text-decoration-none" title="@item.Url">
                                        <i class="bi bi-link-45deg"></i> @(item.Url.Length > 30 ? item.Url.Substring(0, 30) + "..." : item.Url)
                                    </a>
                                }
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Environment))
                                {
                                    var envClass = item.Environment?.ToLower() switch
                                    {
                                        "dev" => "badge-env-dev",
                                        "live" => "badge-env-prod",
                                        "alpha" => "badge-env-qa",
                                        "rc" => "badge-env-staging",
                                        _ => "bg-secondary"
                                    };
                                    <span class="badge @envClass">@item.Environment</span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary me-1" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                      data-confirm="Delete this credential? This action cannot be undone." data-confirm-title="Delete credential">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@if (teacherPortalTypes.Any())
{
    <div class="card mb-4">
        <div class="card-header bg-light border-bottom">
            <h6 class="mb-0 fw-bold text-dark"><i class="bi bi-person-badge"></i> Teacher Portal Credentials</h6>
        </div>
        <div class="table-responsive">
            <table class="table table-hover table-bordered mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th class="text-center" style="width: 12%;">Type</th>
                        <th class="text-center" style="width: 22%;">TPIN</th>
                        <th class="text-center" style="width: 18%;">Password</th>
                        <th class="text-center" style="width: 20%;">URL</th>
                        <th class="text-center" style="width: 12%;">Environment</th>
                        <th class="text-center" style="width: 16%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in teacherPortalTypes)
                    {
                        <tr>
                            <td class="text-center">@item.Type</td>
                            <td class="text-center">
                                @item.Username
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Username', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                <span class="password-hidden" id="pwd-@item.Id">********</span>
                                <span class="password-visible d-none" id="pwd-show-@item.Id">@item.Password</span>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="togglePassword(@item.Id)" title="Show/Hide">
                                    <i class="bi bi-eye" id="eye-@item.Id"></i>
                                </button>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Password', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Url))
                                {
                                    <a href="@item.Url" target="_blank" class="text-decoration-none" title="@item.Url">
                                        <i class="bi bi-link-45deg"></i> @(item.Url.Length > 30 ? item.Url.Substring(0, 30) + "..." : item.Url)
                                    </a>
                                }
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Environment))
                                {
                                    var envClass = item.Environment?.ToLower() switch
                                    {
                                        "dev" => "badge-env-dev",
                                        "live" => "badge-env-prod",
                                        "alpha" => "badge-env-qa",
                                        "rc" => "badge-env-staging",
                                        _ => "bg-secondary"
                                    };
                                    <span class="badge @envClass">@item.Environment</span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary me-1" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                      data-confirm="Delete this credential? This action cannot be undone." data-confirm-title="Delete credential">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@if (otherTypes.Any())
{
    <div class="card mb-4">
        <div class="card-header bg-light border-bottom">
            <h6 class="mb-0 fw-bold text-dark"><i class="bi bi-grid-3x3-gap"></i> Other Type Credentials</h6>
        </div>
        <div class="table-responsive">
            <table class="table table-hover table-bordered mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th class="text-center" style="width: 12%;">Type</th>
                        <th class="text-center" style="width: 22%;">Username</th>
                        <th class="text-center" style="width: 18%;">Password</th>
                        <th class="text-center" style="width: 20%;">URL</th>
                        <th class="text-center" style="width: 12%;">Environment</th>
                        <th class="text-center" style="width: 16%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in otherTypes)
                    {
                        <tr>
                            <td class="text-center">@item.Type</td>
                            <td class="text-center">
                                @item.Username
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Username', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                <span class="password-hidden" id="pwd-@item.Id">********</span>
                                <span class="password-visible d-none" id="pwd-show-@item.Id">@item.Password</span>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="togglePassword(@item.Id)" title="Show/Hide">
                                    <i class="bi bi-eye" id="eye-@item.Id"></i>
                                </button>
                                <button class="btn btn-sm btn-link p-0 ms-1" onclick="copyToClipboard('@item.Password', this)" title="Copy">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Url))
                                {
                                    <a href="@item.Url" target="_blank" class="text-decoration-none" title="@item.Url">
                                        <i class="bi bi-link-45deg"></i> @(item.Url.Length > 30 ? item.Url.Substring(0, 30) + "..." : item.Url)
                                    </a>
                                }
                            </td>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(item.Environment))
                                {
                                    var envClass = item.Environment?.ToLower() switch
                                    {
                                        "dev" => "badge-env-dev",
                                        "live" => "badge-env-prod",
                                        "alpha" => "badge-env-qa",
                                        "rc" => "badge-env-staging",
                                        _ => "bg-secondary"
                                    };
                                    <span class="badge @envClass">@item.Environment</span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary me-1" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                      data-confirm="Delete this credential? This action cannot be undone." data-confirm-title="Delete credential">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@section Scripts {
<script>
function togglePassword(id) {
    var hidden = document.getElementById('pwd-' + id);
    var visible = document.getElementById('pwd-show-' + id);
    var eye = document.getElementById('eye-' + id);
    if (hidden.classList.contains('d-none')) {
        hidden.classList.remove('d-none');
        visible.classList.add('d-none');
        eye.classList.remove('bi-eye-slash');
        eye.classList.add('bi-eye');
    } else {
        hidden.classList.add('d-none');
        visible.classList.remove('d-none');
        eye.classList.remove('bi-eye');
        eye.classList.add('bi-eye-slash');
    }
}
</script>
}
